require('dotenv').config(); const jwt = require('jsonwebtoken'); async function run() { const token = jwt.sign({ id: '64d3b6f2e8f1b6a1e5c3e4a1', role: 'admin' }, process.env.JWT_SECRET, { expiresIn: '1h' }); try { const res = await fetch('http://localhost:5000/api/results/admin/all-attempts', { headers: { Authorization: 'Bearer ' + token } }); const data = await res.json(); console.log('Response:', data); } catch (e) { console.error('Error:', e); } } run();
