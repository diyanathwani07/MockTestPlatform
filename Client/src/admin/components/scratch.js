// scratch file to test parser logic
const mammoth = require("mammoth");

const parseFlashcardsFromHtml = (html) => {
    // We would use DOMParser in browser, but in scratch we just mock it or don't run it
}
