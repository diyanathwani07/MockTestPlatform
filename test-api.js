require('dotenv').config({ path: './Server/.env' }); const mongoose = require('mongoose'); const Result = require('./Server/models/Result'); async function run() { await mongoose.connect(process.env.MONGO_URI); console.log('DB Connected'); const count = await Result.countDocuments(); console.log('Count:', count); const sample = await Result.findOne(); console.log('Sample:', sample); process.exit(); } run().catch(console.error);
