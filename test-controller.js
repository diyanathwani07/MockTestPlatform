require('dotenv').config({ path: './Server/.env' }); const mongoose = require('mongoose'); const { getAllAttempts } = require('./Server/controllers/resultController'); const Result = require('./Server/models/Result'); async function run() { await mongoose.connect(process.env.MONGO_URI); const req = {}; const res = { json: (data) => console.log('Data:', data.totalAttempts, data.attempts.length), status: (code) => ({ json: (d) => console.log('Err:', d) }) }; await getAllAttempts(req, res); process.exit(); } run();
